rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir leitura e escrita para qualquer pessoa autenticada
    // (já que o app é só para duas pessoas, sem autenticação formal)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
